# Output Artifacts

`qp backtest` writes `report.json`, `manifest.json`, `summary.txt`, `equity.csv`, and `fills.csv`. Report schema marker is `qp.backtest.report.v1`; manifest schema marker is `qp.run_manifest.v1`.
