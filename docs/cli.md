# CLI

`qp smoke`, `qp inspect-csv --data <csv>`, `qp validate-config --config <json>`, and `qp backtest --config <json> [--output-dir <dir>]` are implemented. Exit codes: 0 success, 1 runtime, 2 usage, 3 config, 4 data.
