# Labels

Labels are future-only research targets, not decisions. ForwardReturnLabeler selects the first eligible bar after sample.end + prediction_gap and then the first bar covering horizon. TripleBarrierLabeler defaults to conservative stop-loss-wins on same-bar ambiguity.
